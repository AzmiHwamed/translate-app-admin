export function UsersPage() {
  return (
    <div className="page">
      <h1 className="page__title">Users</h1>
      <p className="page__subtitle">Manage users here once the users slice is wired up.</p>

      <div className="placeholder-block">
        Build out usersSlice thunks (list/create/update/delete) and render the table here.
      </div>
    </div>
  );
}
